% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mean_n.R
\name{mean_n}
\alias{mean_n}
\title{Calculate Row Means with Allowed Number of NAs}
\usage{
mean_n(df, n)
}
\arguments{
\item{df}{A data frame or matrix of numeric variables (rows = observations, columns = items).}

\item{n}{An integer specifying the maximum number of allowed NAs in a row to still calculate the mean.}
}
\value{
A numeric vector of row means. Rows with more than \code{n} missing values return \code{NA}.
}
\description{
This function computes the mean of each row in a data frame or matrix,
similar to the SPSS \code{mean.n} function. Unlike the base \code{rowMeans()}
which returns \code{NA} if any values are missing by default, this function allows
a specified tolerance (\code{n}) for the number of \code{NA} values per row.
}
\details{
For example, if you have a 15-item scale and a participant completed only 14,
\code{rowMeans()} would yield \code{NA} for that participant, but \code{mean_n()} can
still return the mean if the number of missing items is less than or equal to \code{n}.
}
\examples{
df <- data.frame(
  item1 = c(1, 2, NA, 4),
  item2 = c(2, NA, NA, 4),
  item3 = c(3, 4, 4, NA)
)
mean_n(df, n = 1)
#> [1] 2 3 NA 4

}
